/:
  get:
    summary: Returns ok message
    description: Optional extended description in CommonMark or HTML.
    responses:
      '200':    # status code
        description: A JSON array of status
        content:
          application/json:
            schema: 
              type: array
              items: 
                type: string